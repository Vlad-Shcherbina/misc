# Ubuntu

```
# install rvm using instructions on their site

source ~/.rvm/scripts/rvm

rvm use 2.0.0

gem install bundler

bundle install

bundle update

bundle exec jekyll serve

# There will be error message from 'execjs', so run
sudo apt-get install nodejs
```
